name: master-test

on:
  push:
    branches:
    - master
    - ombott-dev
  pull_request:
    branches:
    - master

jobs:
  build:

    runs-on: ubuntu-24.04
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13"]

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: pydal_test
          POSTGRES_PASSWORD: pydal_test
          POSTGRES_DB: pydal_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - uses: actions/checkout@v2
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v2
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install Everything
        run: |
            sudo apt-get install memcached libmemcached-tools
            python -m pip install --upgrade pip
            ls -l
            python -m pip install -e .[test]
      - name: Test (SQLite)
        run: |
            python -m unittest tests
      - name: Test (PostgreSQL)
        env:
          DB: postgres://pydal_test:pydal_test@localhost:5432/pydal_test
        run: |
            python -m unittest tests

  postgis-geo:
    runs-on: ubuntu-24.04

    services:
      postgres:
        image: postgis/postgis:16-3.5
        env:
          POSTGRES_USER: pydal_test
          POSTGRES_PASSWORD: pydal_test
          POSTGRES_DB: pydal_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - uses: actions/checkout@v2
      - name: Set up Python 3.12
        uses: actions/setup-python@v2
        with:
          python-version: "3.12"
      - name: Install Everything
        run: |
            python -m pip install --upgrade pip
            python -m pip install -e .[test]
      - name: Test PostgreSQL Geo compiler
        env:
          PYDAL_TEST_POSTGIS: "1"
          DB: postgres://pydal_test:pydal_test@localhost:5432/pydal_test
        run: |
            python -m unittest tests.postgres_geo
